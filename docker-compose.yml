version: '3.8'

services:
  web:
    image: ghcr.io/tdavis6/dailysummaryemail:latest
    container_name: dailySummaryEmail
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped